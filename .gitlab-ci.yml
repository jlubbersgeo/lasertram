# image: python:3.12

# stages:
#     - test

# test-coverage-style:
#     stage: test
#     before_script:
#         - python --version ; pip --version
#         - pip install virtualenv
#         - virtualenv lasertram
#         - source lasertram/bin/activate
#         - pip install -e .
#         - pip install coverage pytest
#     script:
#         - coverage run -m pytest
#         - coverage report

#     coverage: '/(?i)total.*? (100(?:\.0+)?\%|[1-9]?\d(?:\.\d+)?\%)$/'

# USE THIS TO LINT YOUR YAML
# https://code.usgs.gov/<your_group>/<your_project>/-/ci/lint
# https://code.usgs.gov/vsc/petro/tools/lasertram/-/ci/lint

default:
    image: python:3.13
    cache:
        paths:
            - .cache/pip/
    before_script:
        - python --version
        - pip install --upgrade pip
        - mkdir -p "$CI_PROJECT_DIR/.cache/pip"
        - pip install build twine

variables:
    PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"

stages:
    - build
    - test
    - publish-testpypi
    - publish-pypi

build: 
    stage: build
    script:
        - python -m build
    artifacts:
        paths:
            - dist/

test:
    stage: test
    dependencies:
        - build
    script:
        - pip install pytest coverage
        - pip install dist/*.whl
        - coverage run -m pytest
        - coverage report
        - coverage xml -i
        - coverage html
    coverage: '/(?i)total.*? (100(?:\.0+)?\%|[1-9]?\d(?:\.\d+)?\%)$/'
    artifacts:
        reports:
            coverage_report:
                coverage_format: cobertura
                path: coverage.xml
        paths:
            - coverage.xml
            - htmlcov/

publish-testpypi:
    stage: publish-testpypi
    dependencies:
        - build
    script:
        - echo "Uploading to Test PyPI"
        - TWINE_PASSWORD=${TEST_PYPI_TOKEN} TWINE_USERNAME=__token__ python -m twine upload --repository testpypi dist/*
    rules:
        - if: $CI_COMMIT_TAG

publish-pypi:
    stage: publish-pypi
    dependencies:
        - build
    script:
        - echo "Uploading to PyPI"
        - TWINE_PASSWORD=${PYPI_TOKEN} TWINE_USERNAME=__token__ python -m twine upload dist/*
    rules:
        - if: $CI_COMMIT_TAG
        



